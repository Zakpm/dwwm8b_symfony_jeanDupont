<h1>Hi!</h1>

<p>Pour réinitaliser votre mot de passe, veuillez cliquer sur le lien ci-dessous :</p>

<a href="{{ url('visitor.authentication.reset_password', {token: resetToken.token}) }}">{{ url('visitor.authentication.reset_password', {token: resetToken.token}) }}</a>

<p>Ce lien va expirera : {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Ciao!</p>
